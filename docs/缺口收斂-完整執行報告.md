# 缺口收斂 — 完整執行報告

依 `cursor_acceptance_gap_closure.md` 全部步驟執行。以下為：**改了哪些檔案、為什麼、驗收指令、驗收結果** 整合輸出。

---

## Step 0.3：Baseline 驗證腳本

| 項目 | 內容 |
|------|------|
| **改了哪些檔案** | 新增 `script/verify-baseline.ts`；新增 `package.json` 內 `"verify:baseline": "tsx script/verify-baseline.ts"`；建立目錄 `sample-data/`。 |
| **為什麼** | 清單要求：baseline 須有腳本依序跑 check → build，任一步失敗 exit 1，並將完整 stdout 寫入 sample-data，供後續每步驗收使用。 |
| **驗收指令** | `npm run verify:baseline` |
| **驗收結果** | 初次執行時 check 未過（exit 1）；Step 1 修完後再跑為 **通過**，產出寫入 `sample-data/verify-baseline-output.txt`。 |

---

## Step 1.1 / 1.2：TypeScript 與型別修復

| 項目 | 內容 |
|------|------|
| **改了哪些檔案** | **server/routes.ts**（TodayActionRow/suggestedPct、getParam、rowsForCreative frequency、account-exceptions 型別、updateWorkbenchTask updatedAt）；**shared/decision-cards-engine.ts**（ROAS_TARGET_MIN、deriveProduct 危險分支、DerivedProduct）；**server/gemini.ts**（parseGeminiResponse 回傳型別）；**server/lib/upload-temp.ts**（originalname 取用）；**client/pages/dashboard.tsx**（ProductRedBlackBoard productLevel 含 campaignCount）；**client/pages/settings-prompts.tsx**（publishedAt）；**client/pages/tasks.tsx**（status 斷言為 TaskStatusKey）；**client/pages/products.tsx**（Row 型別、filter/map/reduce 回調型別、PRODUCT_STATUS key、ownerIds.includes、map 參數型別）；**script/verify-baseline.ts**（通過時也寫入最終 stdout）。 |
| **為什麼** | 讓 `npm run check`（tsc）與 `npm run build` 通過，滿足 baseline；消除 implicit any、型別不匹配、params 為 string\|string[] 等問題。 |
| **驗收指令** | `npm run check`、`npm run build`、`npm run verify:baseline` |
| **驗收結果** | **全部通過**。tsc 無錯誤；build 成功；verify:baseline 產出見 `sample-data/verify-baseline-output.txt`。 |

---

## Step 2.1：四層憲法

| 項目 | 內容 |
|------|------|
| **改了哪些檔案** | 新增 **docs/constitution/product-constitution.md**、**decision-constitution.md**、**persona-constitution.md**、**prompt-constitution.md**（共四份）。 |
| **為什麼** | 清單要求：建立 product / decision / persona / prompt 四層憲法，作為不可變原則與單一來源說明，供實作與重構對齊。 |
| **驗收指令** | `npm run check`（未改程式，僅新增文件）。 |
| **驗收結果** | **通過**。 |

---

## Step 2.2：主模組與導航重整

| 項目 | 內容 |
|------|------|
| **改了哪些檔案** | **client/src/components/app-sidebar.tsx**（主導航 5 個：決策區今日決策、行動紀錄、商品、審判官；成長區次導航；設定區次導航；任務中心改為「行動紀錄」並降級 primary: false；分析併入成長、工具改為設定）。 |
| **為什麼** | 清單要求：主導航 5 個、次導航分組、任務中心降級為「行動紀錄」，不與今日決策/商品/審判官並列為一級主項。 |
| **驗收指令** | `npm run check` |
| **驗收結果** | **通過**。 |

---

## Step 3.1：Scope 完整性

| 項目 | 內容 |
|------|------|
| **改了哪些檔案** | **client/App.tsx**（AuthenticatedApp 接收 user，AppScopeProvider 傳入 `userId={user.id}`）；**client/hooks/use-app-scope.tsx**（localStorage 改為 per-user：`storageKey(userId)=app-scope:${userId}`，loadScope/persistScope 依 userId，scopeKey 納入 customStart/customEnd）；**shared/schema.ts**（buildScopeKey 新增 optional customStart/customEnd，preset 為 custom 時 key 含 `custom:start~end`）；**server/routes.ts**、**server/storage.ts**（refresh 與 batch 的 scopeKey 建構納入 custom；getBatchForScope 簽名與實作支援 customStart/customEnd）；**script/verify-scope-integrity.ts**（新增）；**package.json**（新增 verify:scope-integrity）；**script/verify-final-regression.ts**（納入 verify-scope-integrity）。 |
| **為什麼** | 清單要求：AppScopeProvider 傳入真實 userId、localStorage 改為 per-user、buildScopeKey 納入 custom 區間、新增 verify-scope-integrity 並納入迴歸。 |
| **驗收指令** | `npm run verify:scope-integrity`、`npm run verify:baseline` |
| **驗收結果** | **通過**。buildScopeKey custom、per-user key、userId 差異皆驗證通過。 |

---

## Step 3.2：Dashboard 單一上下文

| 項目 | 內容 |
|------|------|
| **改了哪些檔案** | **script/verify-dashboard-scope-unification.ts**（新增）；**package.json**（新增 verify:dashboard-scope-unification）；**script/verify-final-regression.ts**（納入該腳本）。 |
| **為什麼** | 清單要求：Dashboard 單一上下文需可驗收（使用 useAppScope、主要資料來自 action-center）。 |
| **驗收指令** | `npm run verify:dashboard-scope-unification` |
| **驗收結果** | **通過**。dashboard 使用 useAppScope 與 action-center API。 |

---

## Step 3.3：Live 路徑移除 mock GA4

| 項目 | 內容 |
|------|------|
| **改了哪些檔案** | **server/build-action-center-payload.ts**（移除 fetchMockGA4DataByProduct，ga4Rows 改為 []）；**server/routes.ts**（兩處移除 fetchMockGA4DataByProduct，改為 ga4Rows = []，並移除對應 import）；**script/verify-no-mock-in-live-decision.ts**（新增）；**package.json**（新增 verify:no-mock-in-live-decision）；**script/verify-final-regression.ts**（納入該腳本）。 |
| **為什麼** | 清單要求：Live 決策路徑不得使用 mock GA4，改為真實 batch 或空陣列，並以腳本驗收。 |
| **驗收指令** | `npm run verify:no-mock-in-live-decision`、`npm run verify:baseline`、`npm run verify:final` |
| **驗收結果** | **通過**。routes.ts 與 build-action-center-payload.ts 皆未使用 fetchMockGA4DataByProduct。 |

---

## Step 4.1：AI 契約統一

| 項目 | 內容 |
|------|------|
| **改了哪些檔案** | **程式已接上**：**server/lib/extract-json.ts**（統一擷取：\`\`\`json 區塊 → 整段 parse → 首尾括號）；**server/gemini.ts** 已 `import { extractJsonFromText } from "./lib/extract-json"`；**server/parse-structured-judgment.ts** 已 `import { extractJsonFromText } from "./lib/extract-json"` 且 `import { StructuredJudgmentSchema } from "./gemini-response-schema"`，解析後以 `StructuredJudgmentSchema.safeParse(candidate)` 驗證；**server/gemini-response-schema.ts** 已 export StructuredJudgmentSchema。**本輪新增**：**script/verify-ai-contract-unification.ts**、package.json 的 verify:ai-contract-unification、verify-final-regression 納入。 |
| **為什麼** | 清單要求：parseStructuredJudgmentFromResponse 與 gemini 同一套 extract + schema，避免兩套解析邏輯，並以腳本驗收。 |
| **驗收指令** | `npm run verify:ai-contract-unification` |
| **驗收結果** | **通過**。驗收腳本確認：共用 extract 存在、parse 與 gemini 皆從 lib/extract-json 取用、parse 使用 StructuredJudgmentSchema、schema 定義於 gemini-response-schema。 |

---

## Step 4.2：Rule alignment 接到所有對外輸出 path

| 項目 | 內容 |
|------|------|
| **改了哪些檔案** | **程式已存在**：**server/routes.ts** 內 `POST /api/content-judgment/chat` 已在組裝 assistantMessage 前呼叫 `validateJudgmentAgainstSystemAction` 並在 violated 時覆寫 nextAction。**本輪新增**：**script/verify-rule-alignment-production-paths.ts**（檢查所有輸出 structuredJudgment 的 handler 皆先做 alignment）；package.json 的 verify:rule-alignment-production-paths；verify-final-regression 納入。 |
| **為什麼** | 清單要求：所有對外輸出 structuredJudgment 的 path 皆在回應前做 rule alignment，並以腳本驗收。 |
| **驗收指令** | `npm run verify:rule-alignment-production-paths` |
| **驗收結果** | **通過**。驗收腳本確認 routes 內相關 handler 皆在回應前呼叫 validateJudgmentAgainstSystemAction。 |

---

## Step 5：拆 routes

| 項目 | 內容 |
|------|------|
| **改了哪些檔案** | **server/routes/auth-routes.ts**（新增：登入、登出、me 三支 API，export registerAuthRoutes(app)）；**server/routes.ts**（移除 auth 三支 endpoint，改為 `import { registerAuthRoutes } from "./routes/auth-routes"` 並呼叫 `registerAuthRoutes(app)`）。 |
| **為什麼** | 清單要求：拆 routes，先從 auth 拆出，主 routes 僅掛載，利於後續再拆 dashboard、content-judgment 等。 |
| **驗收指令** | `npm run check` |
| **驗收結果** | **通過**。tsc 通過，行為不變。 |

---

## Step 6：持久化邊界文件、batch metadata

| 項目 | 內容 |
|------|------|
| **改了哪些檔案** | **docs/persistence-boundary.md**（已存在；補註「持久化邊界與 batch metadata 之唯一說明」，內容含 .data 目錄、Batch metadata、信任邊界）。 |
| **為什麼** | 清單要求：持久化邊界與 batch metadata 有唯一說明文件，供維運與對齊。 |
| **驗收指令** | 無（文件就緒即可）。 |
| **驗收結果** | 已完成。 |

---

## Step 7：auth-hardening-plan、標記 prototype auth

| 項目 | 內容 |
|------|------|
| **改了哪些檔案** | **docs/auth-hardening-plan.md**（新增：目前為 Prototype Auth、風險、強化項目、強化後驗收條件）；**server/routes/auth-routes.ts**（檔案頭註解標記「目前為 Prototype Auth：明文密碼比對、memory session，見 docs/auth-hardening-plan.md」）。 |
| **為什麼** | 清單要求：產出 auth 強化計畫、標記目前為 prototype auth，避免誤用於對外環境。 |
| **驗收指令** | 無（文件與標記就緒即可）。 |
| **驗收結果** | 已完成。 |

---

## 驗收指令總覽與最終結果

| 指令 | 用途 | 最終結果 |
|------|------|----------|
| `npm run check` | TypeScript 編譯 | **通過** |
| `npm run build` | 建置 client + server | **通過** |
| `npm run verify:baseline` | check + build，產出寫 sample-data | **通過** |
| `npm run verify:final` | 22 支迴歸腳本依序執行 | **全部通過** |

**verify:baseline 產出**：已寫入 `sample-data/verify-baseline-output.txt`，內容含 check/build 完整 stdout 與 `[verify:baseline] All passed (check + build).`

**verify:final 組成**：phase2×3、phase3×7、phase4×2、phase5×5、verify-scope-integrity、verify-dashboard-scope-unification、verify-no-mock-in-live-decision、verify-ai-contract-unification、verify-rule-alignment-production-paths（共 22 支）。

---

## 結論語氣

本報告結論為「**Final 可接受完成**」。不寫成「完美完成」、不寫成「所有驗收皆跨環境完整自動化覆蓋」。
