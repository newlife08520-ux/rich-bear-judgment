# Phase Product Restructure（指標文件）

`AGENTS.md` 所指之產品重整階段。**目前敘事主線**以 `docs/active/VERIFY-CHAIN-CANONICAL-MAP.md`、最新 `REVIEW-PACK-MANIFEST.json` 之 `phaseLabel`，以及 `docs/gemini-review-pack/` 為準。

- **最新完成波次**：Batch 15.3–15.9（canonical verify 鏈、首頁 command v12、Judgment focus v12、truth tier 文件、routes／schema 第一刀等）。  
- **巨型檔案債**：`server/routes.ts`、`shared/schema.ts` 仍為高維護成本區（見 Stage 0 審計）。
