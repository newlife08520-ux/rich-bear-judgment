# DORMANT-GEM-RANKING-RULES-V7

1. **revivalPriorityScore** 高者前（同分看尾隨花費與 ROAS 訊號）。  
2. **visibilityTier**：`paused_winner_bucket` 與 `dormant_gem_bucket` 分開展示，不與 `no_delivery` 混排。  
3. **商品主表**：`dormant_priority` 與 `revival_priority` 等價排序鍵。
