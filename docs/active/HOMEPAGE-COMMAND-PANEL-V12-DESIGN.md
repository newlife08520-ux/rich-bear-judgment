# HOMEPAGE-COMMAND-PANEL-V12-DESIGN

- **第一屏**：Top 3 指令、`HomepageCommandPanelV12Chrome` truth tier（Trusted／Reference／Partial 加粗）、scale／rescue／dormant 主帶。  
- **次級**：利潤／Pareto／雷達等收入合 `Collapsible`「營運細節」。  
- **Diagnostics**：`StrategicDiagnosticsCollapsible` 獨立，不與主指令同權重。  
- **截圖約定**：`dashboard-command-panel-v12-{has-data,partial-data,no-data}.png`（`npm run screenshots:page-states` 產出）。
