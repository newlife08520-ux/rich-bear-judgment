# Homepage Hierarchy vNext

**現況 baseline**：`verify:batch98_1:homepage-command-hierarchy-v12`。

## vNext 方向（文件契約）

- 強化 **war-room**：指令層（command）與證據層（truth tier／data confidence）同屏可解析，不重複堆疊同級區塊。
- 實作須通過既有 batch98 verify；新增 UI verify 應 **少量**、避免噪音。
