# UI-TRUTH-MAPPING

- **首頁**：`HomepageCommandPanelV12` 之 Trusted／Reference／Partial 對應 `dataStatus`／`homepageDataTruth`（**Tier B** 擷取對照 `RUNTIME-QUERY-CAPTURES`）。  
- **Judgment**：Focus 模式單條證據為 summary 卡濃縮，非完整引擎輸出。  
- **Dormant**：`visibilityTier` 與 `reasonSummary` 來自 action-center 同批；tier 語意見 `TRUTH-PACK-TIER-MODEL-v2.md`。
