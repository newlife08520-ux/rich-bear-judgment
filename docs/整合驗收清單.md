# 缺口收斂 — 整合驗收清單（最後一次整合看）

依 `cursor_acceptance_gap_closure.md` 執行至結束。本文件供最後一次整合查閱用。

---

## 一、你只要跑這兩道

在專案根目錄執行：

```bash
npm run verify:baseline
npm run verify:final
```

- **verify:baseline**：`npm run check` + `npm run build`，產出寫入 `sample-data/verify-baseline-output.txt`。
- **verify:final**：22 支迴歸腳本依序執行，任一支失敗即 exit 1。

**目前狀態**：兩者皆已跑過且通過（exit 0）。

---

## 二、已完成的步驟（0.3～7）

| Step | 內容 | 產物／備註 |
|------|------|------------|
| **0.3** | Baseline 驗證腳本 | `script/verify-baseline.ts`、`sample-data/`、package.json `verify:baseline` |
| **1.1/1.2** | TypeScript／型別修復 | routes、shared、server、client 多檔；check + build 通過 |
| **2.1** | 四層憲法 | `docs/constitution/`：product、decision、persona、prompt 四個 md |
| **2.2** | 主導航重整 | 主導航 5 個、任務降級為「行動紀錄」、`app-sidebar.tsx` |
| **3.1** | Scope 完整性 | userId 傳入 AppScopeProvider、localStorage per-user、buildScopeKey 含 custom、`verify-scope-integrity.ts` |
| **3.2** | Dashboard 單一上下文 | `verify-dashboard-scope-unification.ts` |
| **3.3** | Live 移除 mock GA4 | routes/build-action-center-payload 移除 fetchMockGA4DataByProduct、`verify-no-mock-in-live-decision.ts` |
| **4.1** | AI 契約統一 | 共用 extract（lib/extract-json）+ StructuredJudgmentSchema、`verify-ai-contract-unification.ts` |
| **4.2** | Rule alignment 全 path | `verify-rule-alignment-production-paths.ts` |
| **5** | 拆 routes | `server/routes/auth-routes.ts`，`registerAuthRoutes(app)` |
| **6** | 持久化邊界 | `docs/persistence-boundary.md`（含 batch metadata） |
| **7** | Auth 計畫與標記 | `docs/auth-hardening-plan.md`、auth-routes 標記 Prototype Auth |

---

## 三、verify:final 的 22 支腳本

1. verify-phase2-acceptance  
2. verify-phase2-lifecycle  
3. verify-phase2-failure-no-pollute  
4. verify-phase3-concurrency  
5. verify-phase3-retry-integration  
6. verify-phase3-no-memory-storage  
7. verify-phase3-upload-cleanup  
8. verify-phase3-workbench-bulk  
9. verify-phase3-retry-wrapped  
10. verify-phase3-event-loop-yield  
11. verify-phase4-gemini-fallback  
12. verify-phase4-schema-validation  
13. verify-phase5-prompt-guardrails  
14. verify-phase5-context-compression  
15. verify-phase5-rule-alignment  
16. verify-phase5-no-contradictory-budget  
17. verify-phase5-production-alignment-path  
18. verify-scope-integrity  
19. verify-dashboard-scope-unification  
20. verify-no-mock-in-live-decision  
21. verify-ai-contract-unification  
22. verify-rule-alignment-production-paths  

---

## 四、關鍵檔案位置

| 用途 | 路徑 |
|------|------|
| 本整合清單 | **docs/整合驗收清單.md**（本檔） |
| 逐步執行紀錄 | docs/acceptance-gap-closure-log.md |
| 藍圖驗收報告 | docs/cursor-rebuild-verification-report.md |
| 四層憲法 | docs/constitution/*.md |
| 持久化邊界 | docs/persistence-boundary.md |
| Auth 強化計畫 | docs/auth-hardening-plan.md |
| Baseline 輸出 | sample-data/verify-baseline-output.txt |
| Auth 路由（已拆） | server/routes/auth-routes.ts |
| 統一 JSON 擷取 | server/lib/extract-json.ts |

---

## 五、結論

- 缺口收斂清單 **Step 0.3～7** 已全部執行，無跳步。  
- **verify:baseline** 與 **verify:final** 均已通過。  
- 是否宣稱「Final 可接受完成」由專案負責人依藍圖條件做最後判定。
