# verify-outputs

- **`verify-product-restructure-20260324.txt`**：`npm run verify:product-restructure` 全文（由本機擷取；若中文亂碼請改以 `npm run capture:verify-outputs` 或終端機 UTF-8 重導）。
- 其他檔可由 **`npm run capture:verify-outputs`** 重新產生（含 `check`、intelligence-suite、7.3–7.6 umbrella、product-restructure）。
