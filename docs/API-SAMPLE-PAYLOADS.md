# API-SAMPLE-PAYLOADS

**Truth tier**：本檔描述之 JSON 範例預設為 **Tier B（seeded-runtime）**，除非段落另有標示。不得視為 Tier C／D 之 staging／prod 真實快照。

- **Action center／dashboard**：見 `docs/RUNTIME-QUERY-CAPTURES/*.sample.json` 與 `docs/SANITIZED-DB-SNAPSHOTS/`；對應欄位以 `shared/schema` 與路由實作為準。  
- **Cross-account summary**：`Tier B` 與首頁 `homepageDataTruth` 欄位一併閱讀 `docs/active/HOMEPAGE-TRUTH-TIER-GUIDE.md`。
